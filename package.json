{
  "name": "cash-monitor",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"nodemon backend/server.js\" \"cd frontend && npm run dev \"",
    "test": "concurrently \"node backend/server.js\" \"cd frontend && npm run dev \"",
    "build": "npm install && cd frontend && npm install && npm run build"
  },
  "keywords": [],
  "author": "Pope",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.0",
    "concurrently": "^7.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.21"
  }
}
